# 작업 정책 (Codex 전용)

이 파일은 Codex가 이 프로젝트에서 작업할 때 반드시 지켜야 하는 규칙을 모아둔 곳입니다. 새 채팅창에서 시작해도 자동으로 로드됩니다.

## 🔒 반영 전 점검 정책

라이브 트레이딩 봇이라 push 전 최소한의 검증은 필요하지만, 비용 차이가 커서 단계를 분리한다.

### 항상 자동 수행 (저렴)
1. **`py_compile`** — 수정한 모든 .py 파일에 대해 한 줄 명령으로 컴파일. 통과 시 `OK_COMPILE` 한 단어로 보고.
2. **변경 파일 목록 확인** — `git status`로 의도하지 않은 파일이 staged 되지 않았는지 한 번 확인.
3. **시간대(KST) / 미사용 import** 등 자명한 부수효과 — 변경 라인을 머릿속으로 review (별도 lint 명령은 굳이 안 돌려도 됨).

### 사용자에게 물어보고 수행 (비쌈)
**스모크 테스트(실제 함수 호출 / assert 검증 스크립트)는 자동으로 돌리지 않는다.** push 직전에 사용자에게 한 줄로 묻는다:

> "py_compile 통과. 스모크 테스트도 돌릴까? (Y/n) — 변경 범위는 [요약]"

사용자가 Y면 테스트 작성·실행, n 또는 "그냥 push"면 바로 push. 답이 모호하면 한 번 더 묻고 임의로 결정하지 않는다.

### 검증 자체를 건너뛰는 경우
- **문서/설정만 변경** (`.md`, `.env.example`, `.gitignore` 등 .py 미포함): py_compile도 생략. 바로 push 가능.
- 사용자가 명시적으로 "검증 생략하고 바로 push"라고 지시: 모두 생략.

### 보고 형식
스모크 테스트를 돌렸을 때만 결과를 한 줄 요약 표로 보고 (항목 / 결과 2컬럼, 10줄 이내). py_compile만 했으면 `OK_COMPILE` 한 줄.

검증 실패 시 push하지 않고 먼저 고친다.

## ⚡ 토큰 효율 정책 (Must)

작업 효율을 위해 다음을 지킨다:

1. **파일은 필요한 부분만 읽기** — 200줄 이상 파일은 `Read`의 `offset`/`limit`로 관련 섹션만. 전체 읽기는 정말 구조 파악이 처음 필요할 때만 1회.
2. **같은 파일 재읽기 금지** — 한 세션에서 이미 읽은 파일은 그 내용을 기억해서 그대로 활용. 직접 수정한 직후 재확인 목적의 재읽기는 하지 않는다 (Edit 결과는 그 자체로 신뢰).
3. **변경 결과를 채팅에 다시 dump하지 않기** — Edit 후 "수정된 부분은 이렇습니다" 식으로 코드 블록을 다시 보여주지 않는다. 파일 경로 + 줄 범위만 표시.
4. **테스트 스크립트는 압축형** — 한 파일에 모든 assert를 모으고, 통과 시 `OK` 한 줄, 실패 시 그 줄만 자세히. 항목별 print 박스는 만들지 않는다.
5. **중간 진행 보고 최소화** — TodoWrite로 진행 상황을 표시할 거면 채팅 텍스트로 같은 내용을 또 풀지 않는다. 단계별 "이제 N단계 합니다" 식 멘트는 생략.
6. **요약 표는 최소 컬럼** — "변경 사항" 같은 요약은 파일 + 한 줄 설명. 라인 번호/링크는 사용자가 요청할 때만.
7. **Plan 단계에서 사용자에게 확인 받은 작업은 곧장 실행** — 같은 계획을 다시 풀어쓰지 않고 바로 코드 변경부터 시작.

## 🕘 시간대 정책

이 프로젝트의 모든 기록/표시 시각은 **KST (Asia/Seoul, UTC+9)** 기준이다. 새 코드를 추가할 때 `datetime.now()` 또는 `date.today()`를 그대로 쓰지 말고 `datetime.now(config.KST)` / `datetime.now(config.KST).date()`를 사용한다. VM이 UTC라 naive `now()`는 UTC를 반환한다.

## 📦 배포 환경

- 운영: GCP free tier VM (Ubuntu 22.04). systemd가 `upbit-trader` / `upbit-dashboard` 두 서비스를 관리하며 부팅 시 자동 시작 + 크래시 시 자동 재시작.
- 배포 경로: 로컬에서 `git push origin main` → GitHub Actions가 VM에 SSH 접속 후 `git pull` + `systemctl restart` 수행.
- 자세한 운영 명령어와 자동 재시작 동작은 [README.md](README.md) 참고.
